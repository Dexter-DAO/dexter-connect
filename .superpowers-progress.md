# SDD progress — 2026-07-03-consent-at-birth-sdk.md
Task 0: complete (lockfile committed, main pushed to origin — 11 commits incl. 0.18.0 source now remote)
Task 1: complete (48db969..55b053d, review approved; mutation-probed float guard, TTL tamper-proof, popup carries no allowance)
Task 2: complete (55b053d..b6f762b, review approved; 4 persistence paths fixed, guards mutation-probed, error propagation preserved)
Task 3: complete (b6f762b..4b3da5c, review approved; CTA gate airtight, no zero/double-fire path, tarball clean). Note: reviewer's 'trivial threshold' honesty nit is unfounded — that language was in the controller dispatch brief, implementer cited it correctly.
  Advisory (consumer pass): chips remain interactive mid-ceremony (benign); third-party popup + panel double-authoring UX wart (hosted page re-collects — by design).
Task 4: BUILT + committed db34dfba, NOT deployed. Money review CLEAR-WITH-CONDITIONS.
  MEDIUM finding: /initialize birth-write is handle-gated, not passkey-signed → plant-a-ceiling griefing, write-once makes it stick, victim's own warmup arms it. No drain. Contradicts USER-AUTHORED ruling on the happy (policy-less-warmup) path.
  HOLD: awaiting Branch decision on the 3 options (create-branch-only restriction / full passkey-sign / accept bearer-handle model). Publish + consumer migration gated on this.
  Also flagged (pre-existing): warmup BODY-path sessionTtlSeconds not server-pinned to 2592000 — recommend pinning regardless of the above.
Task 4: COMPLETE + HARDENED (db34dfba..7a457407). Money re-review CLEAR, no conditions. Plant-onto-existing structurally dead (INSERT-only birth write); all warmup paths pinned 2592000. Decision: create-branch-only + TTL-normalize (Branch: 'whichever is most perfect', no user rigmarole — both fixes 100% server-side, invisible).
Task 5: complete — @dexterai/connect 0.19.0 published (acec4b5), main pushed. dexter-api pushed 7a457407.
