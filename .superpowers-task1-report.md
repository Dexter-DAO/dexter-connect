# Task 1 report — SDK spendPolicy through the create ceremony

**Status:** COMPLETE (committed, NOT pushed, NOT published)

**Commit:** `55b053dfd6ae53d22a9e3900cfd93a4d34353c72`
`feat: consent-at-birth spendPolicy through the create ceremony — user-authored, TTL fixed 30d`
(local only — `main` is ahead 1 of origin; no push, no publish per instructions.)

## Verification (one line)
`npm test` → 11 files / 71 passed (61 baseline + 7 policy + 3 enroll) · `npx tsc --noEmit` clean (exit 0) · `npm run build` success.

## What landed
- `src/policy.ts` (new) — `SESSION_TTL_30D='2592000'`, `SpendPolicy`, `usdToAtomic`, `authoredPolicy`, verbatim per plan.
- `src/policy.test.ts` (new, written FIRST — TDD red confirmed on missing module while the 61 existing stayed green) — ported the proven cases from dexter-fe `birthPolicy.test.ts`: whole dollars, `$`/comma/decimal, `0.000001→1n`, garbage/negative/>6dp/empty rejects, float-divergence magnitudes (`9999999999.999999→9_999_999_999_999_999n`, `999999999999.999999→999_999_999_999_999_999n`), `authoredPolicy('0')→null`, TTL always `2592000`. Adapted names (chipToPolicy→authoredPolicy), dropped authoredAtUnix + storage/parse cases.
- `src/enroll.ts` — `CreateWalletConfig` gains `spendPolicy?: SpendPolicy`; `initializeVault` now builds the body and, when a policy is present, adds `spendLimitAtomic` + `sessionTtlSeconds` alongside the existing `coolingOffSeconds`. TTL is ALWAYS overwritten with `SESSION_TTL_30D` — a caller-tampered `sessionTtlSeconds` can never reach the wire. Popup URL params untouched (allowance is authored on the hosted page).
- `src/enroll.test.ts` (new) — mocked `@simplewebauthn/browser` + mocked fetch (challenge→complete→initialize). Asserts: (a) no policy → body omits `spendLimitAtomic`/`sessionTtlSeconds`; (b) authored policy → body carries `20000000` + `SESSION_TTL_30D`; (c) tampered `sessionTtlSeconds:'999'` → wire still sends `2592000`.
- `src/index.ts` — exports `SESSION_TTL_30D`, `usdToAtomic`, `authoredPolicy` + `type SpendPolicy` (confirmed present in built `dist/index.d.ts`).

## Deviations
None. Plan code used verbatim. Only choice within the plan's latitude: `enroll.test.ts` created fresh (no prior file existed), mocking pattern mirrored from `relay.test.ts`; the inline path is forced with `transport:'inline'` + a stubbed `navigator.credentials` since the node test env has no window.
