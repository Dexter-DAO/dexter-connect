# Task 3 report — AllowanceChips + CreateWalletPanel (./react)

**Status:** DONE. Not published, not pushed (per constraints).

**Commit:** `4b3da5c79d3171bb02a4f0345e1338ee3dbc0eb2`
`feat(react): AllowanceChips + CreateWalletPanel — consent-at-birth is now SDK chrome`
(trailer session id: `session_01Qyd8gy6q982MLESJs78P2H`)

## Verification
- `npm test` → 13 files / 99 tests green (17 new).
- `npx tsc --noEmit` → clean.
- `npm run build` → success; `dist/react.d.ts` exports both `AllowanceChips`/`AllowanceChipsProps` and `CreateWalletPanel`/`CreateWalletPanelProps`.
- Version untouched at 0.18.0 (bump is Task 5).

## What shipped
- `src/AllowanceChips.tsx` — `{ value, onChange }`, radiogroup $5/$20/$50/Custom, NONE preselected, `role=radio`/`aria-checked`, Enter/Space select (Space `preventDefault`), Custom → `inputMode="decimal"` `placeholder="$ amount"` emitting the raw string; presets emit `'5'/'20'/'50'` and close Custom.
- `src/CreateWalletPanel.tsx` — optional name field (label "Name your wallet", maxLength 40, placeholder "Dexter Wallet"), AllowanceChips under "What agents may spend, per 30 days", verbatim fine print, block `DexterButton` CTA "Create your Dexter Wallet" gated on `authoredPolicy(value)` non-null (zero rejected, no default), live `ceremonyPhaseLabel` via `onPhase`, inline `ConnectError` (`.dx-cwp__err`, `role="alert"`) + `onError`, failed run → "Retry" with chips/name preserved, busy guard. Calls `createWallet({ name: trimmedName || 'Dexter Wallet', spendPolicy: authoredPolicy(value)!, apiBase, transport, onPhase })`.
- `src/consentStyles.ts` — new injected-once stylesheet (`STYLE_ID = dexter-connect-consent-styles`), `--dx-*` vars, `.78rem/600/.12em` uppercase chips, ember-gradient active state, sharp corners, no emojis. Mirrors `walletKitStyles`.
- `src/react.ts` — both components + prop types exported.

## Component-testing reality found
The repo's vitest runs in the **default `node` environment** — no `environment` set in `vitest.config.ts`, no jsdom/happy-dom, no @testing-library, and **zero `.test.tsx`** existed (all 11 suites were pure logic). So component testing did **not** exist.

`react-dom` (19.2.7) was already present transitively, which made the plan's named "trivial" DOM path genuinely reachable. I added it rather than skip gating tests, because the CTA gating is the money perimeter:
- **devDep `happy-dom@^15`** + a per-file `// @vitest-environment happy-dom` pragma (exactly the "trivial" threshold named in the task).
- **devDep `@types/react-dom@^19`** — required only so `tsc --noEmit` typechecks the harness (react-dom ships no own types; @types/react-dom was absent). Types-only, dev-only, no config overhaul.
- No `@testing-library` — a 60-line `src/testRender.tsx` uses `react-dom/client` + React 19's `act()`. Not shipped (not a tsup entry; `files: ["dist"]`).

17 tests cover: none-selected-initially, preset emits number + closes Custom, `aria-checked` mapping, Custom reveals decimal input, typing emits raw string, Enter/Space keyboard (+ Space preventDefault), CTA disabled until authored, `custom "0"` stays disabled, valid amount enables, name/label/fine-print composition, `showName=false`, createWallet called with trimmed name + authored policy + apiBase/transport, blank-name default, ConnectError → inline + onError + Retry + state preserved, busy guard (single invocation in flight).

## Deviations
1. Added two dev-only devDeps (`happy-dom`, `@types/react-dom`) to enable the component tests. No prod/runtime dep added; `react-dom` was already present. Judged within the plan's "trivial" bar (no config overhaul).
2. Chip/panel classes namespaced `.dx-allow*` / `.dx-cwp*` in a dedicated `consentStyles.ts` (not `.dx-wchip`, which is the wallet-kit chip). The plan's `.dx-wchip` mention was illustrative; a distinct namespace avoids colliding with the existing wallet chip while honoring the same conventions. Error color `#e5552e` matches the existing `.dx-wmenu__item--danger`.
